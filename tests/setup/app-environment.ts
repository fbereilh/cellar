// Test stub for SvelteKit's `$app/environment`, aliased in vitest.config.ts so
// `.svelte.ts` modules that import it load under the (plugin-less) unit runner.
export const browser = false;
export const dev = false;
export const building = false;
export const version = 'test';

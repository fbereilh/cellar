node_modules/
.venv/
.svelte-kit/
build/
*.log

# Playwright E2E artifacts
test-results/
playwright-report/

# workspace notebook artifact (created when cellar runs in this folder)
notebook.ipynb

# per-project local state: the live port map for `cellar mcp` discovery and the
# single-instance lock (both cleared on exit), plus durable per-folder state that
# is not (remembered ports, checkpoints, UI state, harness allow-list)
.cellar/

# secrets & local machine state (never commit)
.env
.env.*
.databrickscfg
.DS_Store

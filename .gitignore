node_modules/
.venv/
.svelte-kit/
build/
*.log

# workspace notebook artifact (created when cellar runs in this folder)
notebook.ipynb

# ephemeral per-run instance state (live port map for `cellar mcp` discovery +
# single-instance lock)
.cellar/

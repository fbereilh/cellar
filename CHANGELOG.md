# Changelog

All notable changes to Cellar, grouped by release.

<!--
AUTO-GENERATED by git-cliff from the git history - DO NOT EDIT BY HAND.
Regenerate with `make changelog`. Config lives in cliff.toml.
-->

## [0.2.5] - 2026-07-21

### Features

- Databricks-style dbutils.widgets parameter widgets (Phase 0)
- Follow the running cell in the viewed notebook, with opt-out
- Mark running notebooks on their tabs + show live kernel RSS by the status
- 'Databricks runtime' toggle so IS_DATABRICKS notebooks take their widgets path (#7)

### Bug Fixes

- Preserve widget value on redeclare; fix combobox datalist key
- Pin databricks-connect to the cluster's DBR, graceful mismatch error
- Make databricks-connect re-pin asymmetric (only if client newer)
- Self-heal a dead kernel socket instead of wedging the notebook
- Detect a closed Spark Connect session (is_closed) and reconnect instead of falsely reporting connected (#8)

### Documentation

- Add open-source community-health scaffolding
- Sync editor-palette provenance docs with community-health additions
- Docs: note best-effort ipywidgets install in SETUP venv steps
- Sync Databricks Connect version-pin docs in SETUP.md
- Document CELLAR_KERNEL_RECONNECT_TIMEOUT_MS and socket self-heal in SETUP.md
- Cite new e2e test in follow-running-cell docs
- Document CELLAR_KERNEL_MEMORY_POLL_MS env var in SETUP.md

### Miscellaneous

- Restore CLAUDE.md symlink to AGENTS.md

## [0.2.4] - 2026-07-20

### Features

- Bind a SQL cell's result to a named variable via a `-- >> name` line (#153)
- Expose header numbering + report view to the agent (#154)
- Live Spark job-progress bars while a query runs (#157)

### Bug Fixes

- Resolve a cell's uses by load-before-store, not symtable subtraction (#155)
- Probe liveness before the idle watchdog aborts a silent run (#156)

### Documentation

- Stop promising a runtime staleness backstop that does not exist (#152)

## [0.2.3] - 2026-07-15

### Miscellaneous

- Bump checkout/setup-node to v5 (node24) + graceful tap skip without TAP_TOKEN

## [0.2.2] - 2026-07-15

### Bug Fixes

- Make the image build cleanly (uid/gid + multi-arch build stage)

## [0.2.1] - 2026-07-15

### Bug Fixes

- Buildx-compatible uv copy (stage instead of --from=${var})

## [0.2.0] - 2026-07-15

### Features

- Stamp build identity at build time for --version
- Add cellar --help
- User-choosable imports cell, unpinned from the top
- Nbdev-style cell export to a .py module
- Click the cell-id to copy it to the clipboard
- Minimal ipywidgets support for live tqdm progress bars
- Cmd/Ctrl+S saves the notebook (stop Chrome hijacking it)
- Render saved pandas DataFrame HTML tables with the interactive grid
- Clear a cell's output when it's queued, not when it starts
- Show running + queued state per section in the sidebar
- Throttle auto-checkpoints to every N agent actions
- Optional per-level automatic header numbering (display-only)
- Common interactive ipywidgets (sliders, checkbox, dropdown, button, interact) with two-way sync
- CELLAR_ISOLATED env for no-registry, no-reaper launches
- Reproducible, zero-prereq Docker image (pinned kernel env)
- Hide code cells - per-cell + notebook-wide (input-hidden report view)
- Per-agent working notebook - agents edit their own notebook, not the focused tab
- Phase 1 - kernel-per-notebook manager + per-kernel run queue (internals)
- Phase 2 - per-notebook session epoch + run-status doctrine
- Phase 3 - Kernels sidebar shows N kernels with per-kernel controls
- Phase 4 - Databricks per-notebook
- Phase 5 - widgets/inspection/MCP per-notebook + doctrine
- Phase 6 - lifecycle & memory (idle culling, kernel cap, shutdown-on-delete)
- Scale the Kernels section to many kernels (compact rows + bulk actions)
- Add project root to sys.path (default on, Files-section toggle)
- First-class cell/line magics (magic-aware dataflow + rich output)
- %restart_python magic + %run support
- Branch name in Files header + grey gitignored files
- Per-cell git blame in the notebook status bar
- Open and view image files
- Honor hide-all-code in report-style HTML export
- Find_symbol - dataflow-based symbol definition + reference navigation
- Cell_impact - cell dependencies + downstream blast radius
- Auto-reconnect the notebook's session after a kernel restart
- Export_html - agent-facing notebook-to-HTML export (with report style)
- Insert cells between cells (UI insert points + a/b command-mode shortcuts)
- Wipe a notebook's variables from its kernel (Kernels sidebar)
- Refined light-mode palette (color-theory pass to match dark)

### Bug Fixes

- Faster auto-scroll; don't reframe to the imports cell
- Don't combine sdkConfig with clusterId (Spark session refused)
- Never reap a pid-reuse impostor; log every reap + shutdown reason
- Detect expired Spark Connect sessions + auto-reconnect
- Keep long cell runs alive with progress notifications (agent no longer hangs)
- Free agent-session state on disconnect + idle reaper
- Atomic notebook writes (temp+rename) under a per-path write lock
- Guarantee run-queue slot release on the error path
- Pass staleness-probe payload over stdin (fix ARG_MAX on large notebooks)
- Idle watchdog on execute() + restart force-abort (unwedge stalled runs)
- Backpressure - reap a backed-up client instead of buffering the stream in heap
- Evict widget models on comm_close (+ clear on restart)

### Performance

- Cut static token overhead (compact JSON + de-duplicated docs)
- Slim read/output payloads (drop redundant fields, trim tracebacks)
- Compact batch-run result payloads (status summary + errors-in-full)
- Downscale high-DPI plot images on the default read path
- Short cell-id handles + prefix-accepting resolver
- Merge redundant tools (use_notebook, read_cells)
- Cap + coalesce runaway cell output across kernel/SSE/client
- Surgical clean-clone + skip outputs on source-only save
- Compute staleness once per batch run (O(N^2)->O(N))
- Memoize repo detection, cache status/blame, scope blame with -L
- Shared app-wide now-ticker (drop per-cell relative-time intervals)
- Coalesce startup round-trips + suppress internal-probe status broadcasts
- LRU-evict the analysis cache instead of full-flushing at the cap
- Raise git stdout maxBuffer and surface an overflow instead of silent truncation
- Memoize the folding derived on a structural signature
- Report kernel_state busy from the run queue, closing a probe TOCTOU
- Cap search output scan, and read cached databricks on the map path
- Lazily build cell editors on first focus (interim cell virtualization)

### Documentation

- Add brew trust step to install (required for third-party taps)
- Use 2-command trust+install with full reference (auto-taps)
- Lead install with the stable channel, --HEAD as alternative
- Present stable + latest channels as labeled options
- Strip inline # comments from command blocks (break in interactive zsh)
- Drop 'spike' framing from kernel.ts header + AGENTS.md (it's production)
- Kernel-per-notebook architecture + phased migration plan
- Friendlier README with screenshots + polished Docker section

### Miscellaneous

- Sync canonical formula with v0.1.0 stable stanza
- Build + type-check on every PR
- Automate releases (GitHub Release + tap formula bump on v* tag)
- Smoke suite (clean-on-save + run/save/MCP) wired into CI
- Type withProgress local from module signature (fix type-check)
- Add official Svelte MCP for agents working in this repo
- De-hardcode audit + team setup docs

## [0.1.0] - 2026-07-11

### Features

- Zero-config agent connection via `cellar mcp` stdio bridge
- Active-cell emphasis, drag reorder, scrollable outputs, table rendering
- Reframe Connect-an-agent panel around zero-config MCP
- Live agent-run feedback over SSE (P0+P1)
- YAML syntax highlighting for .yml/.yaml files
- Collapsible heading cells
- VS Code-style file management in the workspace tree
- Distinct open_notebook(name) vs create_notebook
- Per-cell last-run badge (who/when/how long)
- One cellar instance per folder + edit-flush on close
- Add add_and_run tool (create + run a cell in one call)
- Taller code-editor cap + per-cell collapse-to-max-height toggle
- Jupyter-style modal keyboard + central shortcut registry
- Follow the agent - scroll to and mark the cell an agent is running
- .toml file support, mirroring the .yml/.yaml path
- Line-level and cell-level change decorations
- One fold state for the Outline and the notebook, a chevron on every heading
- Queue runs on the busy kernel instead of dropping them
- JupyterLab everyday-editing keyboard shortcuts
- Native connect, Unity Catalog browser, and agent tools
- Pinned imports cell, consolidate action, agent import routing
- Persist UI prefs server-side so they survive relaunch
- Interactive DataFrame grid for cell outputs
- Cmd/Ctrl+K command palette
- Collapse all / expand all headings
- Sidebar Environment section + requirements export
- In-app log viewer streaming server/kernel/subprocess logs
- Open, save-as, and convert .py notebooks (jupytext + Databricks)
- Export a notebook to a self-contained HTML file
- Cell staleness + dependency graph, with UI + MCP surface
- Rendered Markdown preview for .md files
- Git blame in the bottom status bar
- Double-click image output to view at natural size, scrollable
- Stop button next to the running spinner to interrupt the kernel
- Clear cell output at execution start on re-run
- Render markdown cells on run; steer agents to add_and_run
- Render Plotly and rich text/html cell outputs
- Agent-facing variable inspection (list_variables + inspect_variable)
- First-class SQL cells (spark.sql → DataFrame grid)
- Notebook snapshots + undo agent actions
- List notebooks loaded in the kernel, not open tabs
- Homebrew tap + cellar --version/--update
- Reap stale/orphaned cellar instances on launch

### Bug Fixes

- Seed venv input from venv dir; dispose manager on rebind
- Strip repo-prefix so git decorations work in subdir workspaces
- Open notebook tab before scrolling to cell from outline/search
- Scope git status to workspace subtree via pathspec
- Fix SVG output data URL and gate empty-state on tab restore
- Short-circuit kernel_state probe when kernel is busy
- Mark cellar launcher executable (git mode 100755)
- Fix SSR default notebook, run guard, and stale active pointer on tab close
- Restore file-tree highlight for active .ipynb tabs
- Fail-fast mcp bridge and pid-guarded runtime cleanup
- Fix outline duplicate-key crash and drag opacity double-dim
- Soften --no-mcp-config fallback banner to observable wording
- Light-mode code editors were rendering dark
- Fix stuck run indicator and per-cell SSE replace state
- Guard runningId clear against overlapping runs; reset lastSeq on load
- Run Jupyter sidecar with cwd=WORKSPACE so kernel cwd is the project
- Soft light-grey background for light-mode code editor
- Render matplotlib figures inline as images
- Create parent dirs on notebook write; fix blank create_notebook name
- Guard setSource no-op, safe remote-edit banner, dedupe createNotebook read
- Fix move-in-place no-op and reject hidden dotfile names in fstree
- Sync notebook doc map on fs delete/rename/move ops
- Keep savedSource in sync on remote edit apply
- Harden instance lock atomicity, savedSource on run, longer attach timeout
- Don't write notebook.ipynb on startup
- Distinguish live-kernel execution from persisted outputs
- Harden run-session epoch against forged, stale, and aborted stamps
- Carry session signal in all read tools; unify epoch rule
- Harden kernel epoch bumps; add error_kernel_unavailable status
- Make error_kernel_unavailable self-expiring; sample the epoch once per read
- Kernels section reflects real runtime state
- Share kernel badge logic, fix green fallback and preview promotion
- Fix first-run kernel badge flash, dedupe kernels card branches
- Honest active-notebook, guarded kernel reads, non-blocking run gate
- Escape defers to CodeMirror, focus follows the selection
- Repair the bad #28/#29 merge - main does not build
- Harden persisted section-order reconciliation
- Make theme toggle instant and fix three light-mode defects
- Flush editor, swap light greys, restore visible text selection
- Native OAuth U2M (external-browser), no cached CLI
- Make cluster listing fast and non-hanging
- Remove zoom cursor on image outputs
- Render agent-created markdown reliably (reactive, not one-shot)
- Keep cell-type menu hidden until the type button is clicked

### Refactor

- Convert core notebook/kernel/run modules + shared types
- Phase 2 - convert remaining server modules to TypeScript
- Phase 3 prep - rename client modules to .ts, add lang=ts to components
- Type staleness/headings/importsRole/sidebarSections/cellLanguage
- Phase 3 - type the client lib modules
- Extend client types.ts with RemoteEdit/UICell/SegHidden
- Phase 3 - type Cell.svelte + output renderers
- Phase 3 - type Level-1 components (panels, chrome, file tree)
- Phase 3 - type Notebook.svelte (pure renderer)
- Phase 3 - type Databricks.svelte panel
- Phase 3 - type Sidebar.svelte + provide typed cellarFileOps context
- Type LiveNotebook.svelte + widen CellRegisterApi.run
- Type +page.svelte + +layout.svelte, add loaded_notebooks to KernelInfo

### Documentation

- Document kernel_state tool and MCP coherence doctrine in README/AGENTS
- Document Makefile targets in AGENTS.md project memory
- Document bin/cellar.js executable bit and make setup chmod
- Sync README architecture diagram with multi-doc live notebooks
- Update AGENTS.md for reframed Connect-an-agent panel
- Docs(agents): repair garbled editor-theming sentence
- Document live agent-run SSE feedback in README
- Docs: note file-tab syntax highlighting incl. yaml
- Document soft light-grey light-mode editor surface
- Document live structural notebook sync in README MCP section
- Note collapsible headings + headings.js shared helper
- Sync fs-op doc-map + hidden-name rules into docs
- Sync README MCP notebook-lifecycle docs with open/create split
- Sync run-metadata (P3) into README + fix stale AGENTS deferred note
- Document single-instance-per-folder and --new flag in README
- Sync docs for single-instance lock and edit-flush
- Docs: sync README with no-startup-notebook behavior
- Record the live-vs-persisted execution invariant
- Note error_kernel_unavailable is derived, not sticky
- Concise, friendly public README

### Miscellaneous

- Prepare for public release (MIT license + metadata)
- Phase 1 - TypeScript tooling (tsconfig + svelte-check)
